SECRET_KEY=your-secret-key-here
# One-time admin promote: set ADMIN_PROMOTE_TOKEN, then visit /auth/promote-admin?token=XXX&email=your@email.com
# ADMIN_PROMOTE_TOKEN=your-secret-token
# ADMIN_PROMOTE_TOKEN=one-time-secret  # Set in Railway, visit /auth/promote-admin?token=xxx to become admin, then remove
DATABASE_URL=sqlite:///apparel.db
STRIPE_PUBLIC_KEY=pk_test_your_stripe_public_key
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret
PAYPAL_CLIENT_ID=your_paypal_client_id
PAYPAL_CLIENT_SECRET=your_paypal_client_secret
PAYPAL_MODE=sandbox
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
# Only this email can access Admin; all other accounts get customer portals only
ADMIN_EMAIL=purposefullymadekc@gmail.com

# S&S Activewear API (products linked by style_number; sync from Admin → Products)
# SSACTIVEWEAR_API_KEY=your_api_key
# SSACTIVEWEAR_ACCOUNT_NUMBER=your_account_number
# SSACTIVEWEAR_API_URL=https://api.ssactivewear.com
UPLOAD_FOLDER=static/uploads
MAX_CONTENT_LENGTH=16777216
SHIPPING_FLAT_RATE=11.00

# Text alerts for design requests - choose ONE option:
# Option A: Email-to-SMS (no extra platform) - uses your MAIL_* settings above
ADMIN_PHONE=7852491464
ADMIN_PHONE_CARRIER=verizon
# Carriers: verizon, att, tmobile, sprint, uscellular, cricket, boost, virgin, metro

# Option B: Twilio (if you prefer)
# TWILIO_ACCOUNT_SID=your_twilio_account_sid
# TWILIO_AUTH_TOKEN=your_twilio_auth_token
# TWILIO_PHONE_NUMBER=+1234567890

# Cloudflare R2 - cloud image storage (free tier; prevents image loss on server restarts)
# Dashboard → R2 Object Storage → create a bucket, then create an API token with Edit access
R2_ACCOUNT_ID=your_cloudflare_account_id
R2_ACCESS_KEY_ID=your_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key
R2_BUCKET_NAME=kb-apparel-uploads
# After creating the bucket: R2 → bucket → Settings → Public Access → Enable
# Then paste the r2.dev URL it gives you (or a custom domain you connect)
R2_PUBLIC_URL=https://pub-xxxxxxxxxxxxxxxx.r2.dev
