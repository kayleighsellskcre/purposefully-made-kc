# Test-only dependencies. Not installed on Railway, so the production image
# stays small and nothing test-related can run in production.
#   py -3.12 -m pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0.0
